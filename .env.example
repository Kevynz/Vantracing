# Environment Configuration / Configuração de Ambiente
# Copy this file to .env and fill in your actual values
# Copie este arquivo para .env e preencha com seus valores reais
# 
# IMPORTANT: Never commit .env to version control!
# IMPORTANTE: Nunca faça commit do .env no controle de versão!

# ============================================================================
# DATABASE CONFIGURATION / CONFIGURAÇÃO DO BANCO DE DADOS
# ============================================================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=vantracing_db
DB_USER=root
DB_PASSWORD=your_password_here
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# ============================================================================
# APPLICATION SETTINGS / CONFIGURAÇÕES DA APLICAÇÃO
# ============================================================================
APP_NAME=VanTracing
APP_ENV=production
APP_DEBUG=false
APP_URL=http://localhost/vantracing

# Generate using: php -r "echo bin2hex(random_bytes(32));"
# Gere usando: php -r "echo bin2hex(random_bytes(32));"
APP_KEY=your_generated_key_here_64_characters_long

# Application Version / Versão da Aplicação
APP_VERSION=2.0.0

# ============================================================================
# SESSION CONFIGURATION / CONFIGURAÇÃO DE SESSÃO
# ============================================================================
SESSION_LIFETIME=7200
SESSION_DRIVER=file
SESSION_SECURE=false
SESSION_HTTPONLY=true
SESSION_SAMESITE=lax

# ============================================================================
# EMAIL CONFIGURATION / CONFIGURAÇÃO DE EMAIL
# ============================================================================
# For Gmail, enable "Less secure app access" or use App Password
# Para Gmail, habilite "Acesso a app menos seguro" ou use Senha de App

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@vantracing.com
MAIL_FROM_NAME=VanTracing

# Alternative: SendGrid / Alternativa: SendGrid
# MAIL_HOST=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your_sendgrid_api_key

# Alternative: Mailgun / Alternativa: Mailgun
# MAIL_HOST=smtp.mailgun.org
# MAIL_PORT=587
# MAIL_USERNAME=postmaster@your-domain.mailgun.org
# MAIL_PASSWORD=your_mailgun_password

# ============================================================================
# SECURITY / SEGURANÇA
# ============================================================================
# Password Reset Token Expiration (in minutes)
# Expiração do Token de Redefinição de Senha (em minutos)
PASSWORD_RESET_EXPIRATION=60

# Password Requirements / Requisitos de Senha
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBERS=true
PASSWORD_REQUIRE_SPECIAL=true

# Rate Limiting (requests per minute) / Limitação de Taxa
RATE_LIMIT_LOGIN=5
RATE_LIMIT_API=60

# CORS Settings / Configurações CORS
CORS_ALLOWED_ORIGINS=http://localhost,https://yourdomain.com
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE

# ============================================================================
# FILE UPLOAD CONFIGURATION / CONFIGURAÇÃO DE UPLOAD DE ARQUIVOS
# ============================================================================
# Max upload size in bytes (5MB = 5242880) / Tamanho máximo em bytes
MAX_UPLOAD_SIZE=5242880
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp
ALLOWED_DOCUMENT_TYPES=pdf,doc,docx

# Upload directories / Diretórios de upload
UPLOAD_PATH=uploads/
AVATAR_PATH=uploads/avatars/
DOCUMENTS_PATH=uploads/documents/

# ============================================================================
# GOOGLE MAPS API / API DO GOOGLE MAPS
# ============================================================================
# Required for location tracking / Necessário para rastreamento de localização
# Get your key at: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Google Maps Settings / Configurações do Google Maps
GOOGLE_MAPS_DEFAULT_LAT=-23.5505
GOOGLE_MAPS_DEFAULT_LNG=-46.6333
GOOGLE_MAPS_DEFAULT_ZOOM=12

# ============================================================================
# FIREBASE (OPTIONAL) / FIREBASE (OPCIONAL)
# ============================================================================
# For real-time notifications and push notifications
# Para notificações em tempo real e push notifications
# Get your config at: https://console.firebase.google.com/

FIREBASE_ENABLED=false
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================================================
# PUSH NOTIFICATIONS (OPTIONAL) / NOTIFICAÇÕES PUSH (OPCIONAL)
# ============================================================================
PUSH_ENABLED=false
FIREBASE_SERVER_KEY=your_firebase_server_key
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# ============================================================================
# SENTRY - ERROR TRACKING (OPTIONAL) / RASTREAMENTO DE ERROS (OPCIONAL)
# ============================================================================
# Get your DSN at: https://sentry.io/
SENTRY_ENABLED=false
SENTRY_DSN=your_sentry_dsn_here
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1

# ============================================================================
# ANALYTICS (OPTIONAL) / ANÁLISES (OPCIONAL)
# ============================================================================
# Google Analytics
ANALYTICS_ENABLED=false
GOOGLE_ANALYTICS_ID=UA-XXXXX-Y
GOOGLE_TAG_MANAGER_ID=GTM-XXXXXXX

# Alternative: Matomo / Alternativa: Matomo
# MATOMO_URL=https://your-matomo-instance.com
# MATOMO_SITE_ID=1

# ============================================================================
# SMS SERVICE (OPTIONAL) / SERVIÇO DE SMS (OPCIONAL)
# ============================================================================
# Twilio for SMS notifications / Twilio para notificações SMS
SMS_ENABLED=false
TWILIO_SID=your_twilio_account_sid
TWILIO_TOKEN=your_twilio_auth_token
TWILIO_FROM=+1234567890

# Alternative: Nexmo/Vonage / Alternativa: Nexmo/Vonage
# NEXMO_KEY=your_nexmo_api_key
# NEXMO_SECRET=your_nexmo_api_secret
# NEXMO_FROM=VanTracing

# ============================================================================
# PAYMENT GATEWAY (OPTIONAL) / GATEWAY DE PAGAMENTO (OPCIONAL)
# ============================================================================
# Stripe
STRIPE_ENABLED=false
STRIPE_PUBLIC_KEY=pk_test_your_public_key
STRIPE_SECRET_KEY=sk_test_your_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# PayPal
PAYPAL_ENABLED=false
PAYPAL_MODE=sandbox
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_SECRET=your_paypal_secret

# PagSeguro (Brazil) / PagSeguro (Brasil)
PAGSEGURO_ENABLED=false
PAGSEGURO_EMAIL=your_pagseguro_email
PAGSEGURO_TOKEN=your_pagseguro_token
PAGSEGURO_ENV=sandbox

# ============================================================================
# BACKUP CONFIGURATION / CONFIGURAÇÃO DE BACKUP
# ============================================================================
BACKUP_ENABLED=false
BACKUP_PATH=backups/
BACKUP_SCHEDULE=daily
BACKUP_RETENTION_DAYS=30

# ============================================================================
# CACHE CONFIGURATION / CONFIGURAÇÃO DE CACHE
# ============================================================================
CACHE_DRIVER=file
CACHE_PREFIX=vantracing_
CACHE_TTL=3600

# Redis (optional) / Redis (opcional)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=null
REDIS_DATABASE=0

# ============================================================================
# QUEUE CONFIGURATION / CONFIGURAÇÃO DE FILA
# ============================================================================
QUEUE_DRIVER=sync
QUEUE_CONNECTION=sync

# ============================================================================
# LOCALIZATION / LOCALIZAÇÃO
# ============================================================================
# Default language / Idioma padrão
DEFAULT_LOCALE=pt
FALLBACK_LOCALE=pt
AVAILABLE_LOCALES=pt,en

# Timezone / Fuso Horário
APP_TIMEZONE=America/Sao_Paulo
# Other options: America/New_York, Europe/London, etc.

# ============================================================================
# LOGGING / REGISTRO DE LOGS
# ============================================================================
LOG_LEVEL=error
LOG_CHANNEL=daily
LOG_PATH=logs/app.log
LOG_MAX_FILES=30

# ============================================================================
# DEVELOPMENT / DESENVOLVIMENTO
# ============================================================================
# Only for development environment / Apenas para ambiente de desenvolvimento
DEV_MODE=false
SHOW_SQL_QUERIES=false
ENABLE_PROFILER=false

# ============================================================================
# API CONFIGURATION / CONFIGURAÇÃO DA API
# ============================================================================
API_PREFIX=api
API_VERSION=v1
API_RATE_LIMIT=60

# ============================================================================
# WEBSOCKET / REAL-TIME (OPTIONAL)
# ============================================================================
WEBSOCKET_ENABLED=false
WEBSOCKET_HOST=127.0.0.1
WEBSOCKET_PORT=6001
WEBSOCKET_SSL=false

# Pusher (optional) / Pusher (opcional)
PUSHER_APP_ID=your_pusher_app_id
PUSHER_APP_KEY=your_pusher_key
PUSHER_APP_SECRET=your_pusher_secret
PUSHER_APP_CLUSTER=mt1

# ============================================================================
# SOCIAL LOGIN (OPTIONAL) / LOGIN SOCIAL (OPCIONAL)
# ============================================================================
# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost/vantracing/auth/google/callback

# Facebook OAuth
FACEBOOK_CLIENT_ID=your_facebook_app_id
FACEBOOK_CLIENT_SECRET=your_facebook_app_secret
FACEBOOK_REDIRECT_URI=http://localhost/vantracing/auth/facebook/callback

# ============================================================================
# CDN CONFIGURATION (OPTIONAL) / CONFIGURAÇÃO DE CDN (OPCIONAL)
# ============================================================================
CDN_ENABLED=false
CDN_URL=https://cdn.yourdomain.com
AWS_S3_BUCKET=your_bucket_name
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_DEFAULT_REGION=us-east-1

# ============================================================================
# MAINTENANCE MODE / MODO DE MANUTENÇÃO
# ============================================================================
MAINTENANCE_MODE=false
MAINTENANCE_MESSAGE=Site under maintenance / Site em manutenção
MAINTENANCE_RETRY_AFTER=3600

# ============================================================================
# FEATURE FLAGS / FLAGS DE FUNCIONALIDADES
# ============================================================================
FEATURE_REGISTRATION=true
FEATURE_PASSWORD_RESET=true
FEATURE_EMAIL_VERIFICATION=false
FEATURE_TWO_FACTOR_AUTH=false
FEATURE_LOCATION_TRACKING=true
FEATURE_NOTIFICATIONS=true
FEATURE_CHAT=false

# ============================================================================
# NOTES / NOTAS
# ============================================================================
# After copying this file to .env, make sure to:
# Depois de copiar este arquivo para .env, certifique-se de:
#
# 1. Update all credentials with real values
#    Atualizar todas as credenciais com valores reais
#
# 2. Generate a strong APP_KEY
#    Gerar uma APP_KEY forte
#
# 3. Never commit .env to version control
#    Nunca fazer commit do .env no controle de versão
#
# 4. Keep .env secure with proper file permissions
#    Manter .env seguro com permissões de arquivo adequadas
#
# 5. Use different .env files for different environments
#    Usar arquivos .env diferentes para ambientes diferentes

LOG_LEVEL=error
LOG_PATH=logs/app.log
